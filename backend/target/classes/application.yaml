spring:
  application:
    name: giayhieu
  datasource:
    url: jdbc:mysql://localhost:3306/giayhieu?useSSL=false&serverTimezone=UTC
    username: root
    password: "tribao123"
    driver-class-name: com.mysql.cj.jdbc.Driver  # ← Thêm dòng này

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true  # ← Optional: xem SQL logs

server:
  port: 8090
