spring:
  application:
    name: giayHieu
  datasource:
    url: jdbc:mysql://localhost:3308/giayHieu?useSSL=false&serverTimezone=UTC
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver  # ← Thêm dòng này

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true  # ← Optional: xem SQL logs

server:
  port: 8090
